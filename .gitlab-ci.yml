# This file is a template, and might need editing before it works on your project.
# This is a sample GitLab CI/CD configuration file that should run without any modifications.
# It demonstrates a basic 3 stage CI/CD pipeline. Instead of real tests or scripts,
# it uses echo commands to simulate the pipeline execution.
#
# A pipeline is composed of independent jobs that run scripts, grouped into stages.
# Stages run in sequential order, but jobs within stages run in parallel.
#
# For more information, see: https://docs.gitlab.com/ee/ci/yaml/index.html#stages
#
# You can copy and paste this template into a new `.gitlab-ci.yml` file.
# You should not add this template to an existing `.gitlab-ci.yml` file by using the `include:` keyword.
#
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Getting-Started.gitlab-ci.yml

stages:          # List of stages for jobs, and their order of execution
  - configure
  - build
  - test
  - coverage
  - deploy

variables:
  BUILD_DIR: build-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHA}
  COVERAGE_DIR: coverage
  SRC_DIR: ${CI_PROJECT_DIR}

build-job:       # This job runs in the build stage, which runs first.
  stage: build
  tags: ['ci.inria.fr', 'small'] # Inria's shared runners
  image: ubuntu:latest # By default, an image of busyBox (minimal linux with no package manager and no compiler) is loaded 

  script:
    - echo "Compiling the code..."
    - apt update -y
    - apt install g++ libopenmpi-dev gcovr -y
    - mkdir -p $BUILD_DIR
    - gcc -D BRANCH_1 -O0  -fPIC --coverage -g -O0 -o $BUILD_DIR/a.out hello-world.c
    - echo "Compile complete."
    - cd $BUILD_DIR
    - ./a.out 2 5
    - ./a.out 5 2
    - mkdir -p $COVERAGE_DIR
    - mkdir -p $COVERAGE_DIR/cobertura
    - mkdir -p $COVERAGE_DIR/html
    - >-
     gcovr . -r ${SRC_DIR} --exclude-unreachable-branches --print-summary --txt
     --cobertura-pretty --cobertura $COVERAGE_DIR/cobertura/coverage.xml

  coverage: '/^TOTAL.*\s+(\d+\%)$/'

  artifacts:
    name: ${CI_JOB_NAME}-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHA}
    expire_in: 2 days
    reports:
      coverage_report:
        coverage_format: cobertura
        path: $COVERAGE_DIR/cobertura/coverage.xml

    reports:
      coverage_report:
        coverage_format: cobertura
        path: $COVERAGE_DIR/coverage.xml

#unit-test-job:   # This job runs in the test stage.
#  stage: test    # It only starts when the job in the build stage completes successfully.
#  tags: ['ci.inria.fr', 'small'] # Inria's shared runners
#  image: ubuntu:latest # By default, an image of busyBox (minimal linux with no package manager and no compiler) is loaded 

#  script:
#    - echo "Running unit tests... This will take about 60 seconds."
#    - cd $BUILD_DIR
#    - ./a.out 2 5
#    - ./a.out 5 2
#    - echo "Success"
#  artifacts:
#    expire_in: 2 days
#    paths: ['$BUILD_DIR/*.gcno','$BUILD_DIR/*.gcda']

#lint-test-job:   # This job also runs in the test stage.
#  stage: coverage    # It can run at the same time as unit-test-job (in parallel).
#  tags:
#    - ci.inria.fr
#    - small
#  image: ubuntu:latest # By default, an image of busyBox (minimal linux with no package manager and no compiler) is loaded 

#  script:
#    - apt update -y
#    - apt install -y g++ gcovr
#    - cd $BUILD_DIR
#    - ls
#    - mkdir -p $COVERAGE_DIR
#    - gcovr --xml-pretty --exclude-unreachable-branches --print-summary -o $COVERAGE_DIR/coverage.xml --root ${CI_PROJECT_DIR}
  
#  coverage: '/^TOTAL.+?(\d+\%)$/'

#  artifacts:
#    name: ${CI_JOB_NAME}-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHA}
#    expire_in: 2 days
#    reports:
#      coverage_report:
#        coverage_format: cobertura
#        path: $COVERAGE_DIR/coverage.xml

#deploy-job:      # This job runs in the deploy stage.
#  stage: deploy  # It only runs when *both* jobs in the test stage complete successfully.
#  tags:
#    - ci.inria.fr
#    - small
#  environment: production
#  script:
#    - echo "Deploying application..."
#    - echo "Application successfully deployed."
